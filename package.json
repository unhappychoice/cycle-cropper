{
  "name": "cycle-cropper",
  "version": "1.1.18",
  "description": "Cycle.js component for cropper.js",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "./node_modules/.bin/tslint -c tslint.json src/**/*.ts",
    "watch:lint": "chokidar 'src/**/*.ts' -c 'npm run lint'",
    "watch:build": "chokidar 'src/**/*.ts' -c 'npm run build'",
    "watch": "chokidar 'src/**/*.ts' -c 'npm run lint && npm run build'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/unhappychoice/cycle-cropper.git"
  },
  "keywords": [
    "cropper.js",
    "crop",
    "image"
  ],
  "author": "unhappychoice",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/unhappychoice/cycle-cropper/issues"
  },
  "homepage": "https://github.com/unhappychoice/cycle-cropper#readme",
  "dependencies": {
    "@types/cropperjs": "^1.1.0",
    "cropperjs": "^2.1.0"
  },
  "peerDependencies": {
    "@cycle/dom": "^22.*",
    "@cycle/run": "^5.*",
    "xstream": "^11.*"
  },
  "devDependencies": {
    "@cycle/dom": "^23.0.0",
    "@cycle/run": "^5.2.0",
    "chokidar-cli": "^3.0.0",
    "tslint": "^6.0.0",
    "typescript": "^6.0.2",
    "xstream": "^11.*"
  }
}
